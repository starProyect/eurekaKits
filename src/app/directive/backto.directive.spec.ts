import { BacktoDirective } from './backto.directive';

describe('BacktoDirective', () => {
  it('should create an instance', () => {
    const directive = new BacktoDirective();
    expect(directive).toBeTruthy();
  });
});
